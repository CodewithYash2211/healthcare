rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Auth has been removed from this app - all access is open.
    // For production, re-add authentication and restrict these rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
